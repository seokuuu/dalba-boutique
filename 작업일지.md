# 작업일지 - 2026.04.08

## 작업 파일
- `index.html` (고도몰 `메인/main/test_260408.php`)

---

## 적용된 변경사항

### 1. 메인 배너 (section_01)

**텍스트 크기 통일** (4개 슬라이드 공통, inline style)
- `.category_tit`: font-size: 24px, font-weight: 300
- `h2` 메인 타이틀: font-size: 74px, font-weight: 300
- `.extra_txt`: font-size: 24px, font-weight: 300

**텍스트 컬러** — 4개 슬라이드 전부 `#000` (블랙) 통일

**4번 슬라이드 (올쎄라 더블샷)**
- 타이틀 영문으로 변경: "무제한 샷 올인원 토탈 케어 올쎄라 더블샷" → "d'Alba Allthera Double Shot"
- 슬라이드 순서 맨끝(4번)으로 이동

**버튼 스타일** (`<style>` 블록)
- 기본: `color: #000`, `border-color: #000`
- hover: `background: #000`, `color: #fff`, `border-color: #000`

**배너 높이**
- `min-height: calc(100vh - 60px)` — 풀스크린

### 2. 베스트 프로덕트 (section_03)

**텍스트 컬러** — `data-text-color="#fff"` → `"#000"` (블랙)

**버튼 hover** — `background: #000`, `color: #fff`, `border-color: #000`

**좌우 화살표** — 검정색, 50px, 투명 배경

### 3. 공통

**a태그 밑줄 제거** — section_01, section_01-2, section_02, section_03, collection_common, section_06

### 4. 텍스트 크기 반응형 처리

- PC(901px 이상)만 font-size 적용, 모바일은 고도몰 테마 원본 유지
- `@media screen and (min-width: 901px)` 로 감싸서 처리

---

## GEO 액션 진단 반영 (2026.04.10)

### 1. 메타태그 중복 해소 — 완료
- 고정 텍스트 메타태그 2벌 중복 → 고도몰 변수 1벌로 통합
- 파일: `outline/_header.html`

### 2. 파비콘 robots.txt — 완료
- `Disallow: /data/` 하단에 `Allow: /data/skin/front/dalba2/img/favicon/` 추가
- PC / 모바일 둘 다 적용

### 3. JSON-LD 구조화 데이터 — 완료
- 상품 상세페이지에 Product schema JSON-LD 추가
- 파일: `goods/goods_view.html`

### 4. 이미지 alt 속성 — 완료
- 빈 alt 속성에 상품명을 자동 적용하는 스크립트 추가
- 파일: `goods/goods_view.html`

### 5. OG 태그 중복 제거 — 완료
- 상단 레이아웃의 고정 OG태그 삭제, 고도몰 자동 생성으로 대체
- 파일: `outline/_header.html`

### 6. HTML 구조 정리 — 완료
- 중복 `<title>` 태그 제거, 메타태그 계층 구조 정리
- 파일: `outline/_header.html`

### GEO 반영 대상 파일
| 파일 | 고도몰 위치 |
|---|---|
| `_header.html` | 전체 레이아웃 > `outline/_header.html` |
| `goods_view.html` | 상품 > `goods/goods_view.html` |
| robots.txt | 고도몰 관리자에서 직접 수정 |

---

## GEO 반영 후 장애 대응 (2026.04.13)

### 이슈 1: 상품 상세페이지 구매/리뷰/선물하기 버튼 전부 미동작
- **원인**: `goods_view.html` 의 JS 문자열 리터럴 안에 raw newline이 삽입되어 `Uncaught SyntaxError: Invalid or unexpected token` 발생 → `var goodsNo` 선언부 이전에 스크립트 중단 → 이벤트 핸들러 바인딩 실패
- **근본 원인**: IDE 자동 포매팅이 고도몰 템플릿 파일 편집 중 문자열 안에 개행 삽입
- **수정**: `goods_view.html` 내 6곳의 raw newline 제거 (한 줄로 합침)
  - L203 `window.open` 옵션
  - L338 `couponApplyHtml`
  - L556 `$("#cart_tab_option ...")` 셀렉터
  - L736, L762, L787 alert 메시지

### 이슈 2: 상품 옵션 선택 후 -/+/x 버튼 전부 미동작
- **원인**: `_header.html` line 167의 `< !--{ / }-->` (공백 삽입) 때문에 `<script>` 블록 syntax error → `gdCurrencySymbol` 등 전역 변수 선언 실패 → `goodsViewController.option_price_display` 호출 시 TypeError
- **근본 원인**: IDE 자동 포매팅이 `<!--` → `< !--` 로 변질시킴
- **수정**: `_header.html` 의 `< !--` 13곳 전부 `<!--` 로 복구
  - script 블록 내 1곳 (line 167) — 버튼 이슈 직접 원인
  - style 블록 내 12곳 (line 185~249) — CSS 조건부 스타일 복원

### 이슈 3: 선물하기 클릭 핸들러 내 코드 누락
- **원인**: `goods_view.html` `$('.present').on('click', …)` 핸들러 내부가 한 줄 주석(`//`) 뒤에 코드가 붙은 패턴으로 깨져있어 주석 뒤 코드 전체가 주석 처리됨
- **수정**: 각 문장을 개별 라인으로 분리

### CLAUDE.md 에 재발 방지 가이드 추가
- 고도몰 템플릿 파일 편집 시 자동 포매팅 금지
- 편집 후 `< !--`, `-- >`, JS 문자열 내 줄바꿈 패턴 grep 검증 필수
- 브라우저 DevTools Console 확인이 유일한 검증 수단

---

## GEO 팀 점검 피드백 대응 (2026.04.13)

GEO 팀 점검 후 추가 피드백 수령. 우리 코드 수정이 아닌 **컨텐츠 팀 입력** 영역으로 확인됨.

### 컨텐츠 팀 전달 요청 사항
1. **상품 "짧은 설명" 필드 입력** — 현재 공백이라 메타 description / JSON-LD description 공백으로 렌더링 중. 상품별 60~80자 핵심 메시지 입력 필요
2. **상품 상세 이미지 alt 텍스트 입력** — 통이미지에 담긴 내용을 풀어쓴 alt 작성 필요 (업로드 시점에 입력)
3. **페이지별 SEO 설정 보완** — 카테고리/브랜드/기획전/게시판 SEO 설정 미입력 상태

### 확인된 정상 동작
- 메타태그 중복 해소 (1번) 완료 확인
- 파비콘 robots.txt (2번) 완료 확인
- OG 태그 중복 (5번) 완료 확인
- HTML `<title>` (6번) 상품 상세 정상 렌더링 확인 (`{상품명} - 달바부티크` 형태)

---

## 보류 / 미반영
- 이미지 변경: 내부 컨펌 진행 중, 확정 후 CSS로 직접 수정 예정
- 인스타그램 영역 (section_08) 자동 업데이트: 보류
- 모바일 전용 이미지: 미확보
- 컨텐츠 팀 입력 작업 3건: 상품 짧은 설명 / 이미지 alt / 페이지별 SEO 설정
