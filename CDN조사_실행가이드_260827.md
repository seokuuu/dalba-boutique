# CDN 절대 URL — DB 전수 조사 실행 가이드

작성일: 2026-08-27
목적: PHP 8 업그레이드 대비, DB에 저장된 절대 CDN URL의 **정확한 대상 범위와 건수** 확보
전제: `SELECT`만 실행. DB 무변경. 치환(`UPDATE`)은 NHN 답변 확인 후 별도 진행

---

## 0. 상황 요약

- 달바부티크 = **Pro 서버** (`cdn-pro-web-223-233.cdn-nhncommerce.com`) → 점검 대상 확정
- 프론트 크롤링 결과: 샘플 상품 15개 **전부** 절대 URL 포함, 메인 배너에도 26건
- 전체 규모는 **추정 1,000건 이상** — 정확한 수치는 DB로만 확정 가능
- NHN 제시 쿼리는 `es_goods.goodsDescription` 한 필드만 봐서 **범위가 부족함**

---

## 1. 가장 빠른 길 — DB에 직접 붙을 수 있다면

phpMyAdmin이든 mysql 클라이언트든 **DB에 붙을 수만 있으면 아래 SQL 3개면 끝납니다.** PHP 스크립트 만들 필요 없습니다.

### 1-1. 어떤 테이블·컬럼에 절대 URL이 있는지 전수 탐색

먼저 이 쿼리로 **검사할 SQL을 생성**합니다. (직접 검사하는 게 아니라 SQL 문자열을 만들어냅니다)

```sql
SELECT GROUP_CONCAT(
  CONCAT(
    'SELECT ''', TABLE_NAME, ''' AS tbl, ''', COLUMN_NAME, ''' AS col, COUNT(*) AS cnt ',
    'FROM `', TABLE_NAME, '` WHERE `', COLUMN_NAME, '` LIKE ''%cdn-pro-web%'''
  )
  SEPARATOR ' UNION ALL '
) AS generated_sql
FROM information_schema.COLUMNS
WHERE TABLE_SCHEMA = DATABASE()
  AND DATA_TYPE IN ('varchar','text','mediumtext','longtext','tinytext');
```

> ⚠️ 결과가 길면 잘릴 수 있습니다. 잘리면 실행 전에 아래를 먼저 돌려주세요.
> ```sql
> SET SESSION group_concat_max_len = 1000000;
> ```

**출력된 SQL을 복사해서 그대로 실행**하면, 전체 DB에서 절대 URL이 들어있는 테이블·컬럼과 건수가 한 번에 나옵니다.

### 1-2. 건수 0인 것 걸러내고 정리

1-1 결과에서 `cnt > 0` 인 행만 추리면 그게 **최종 대상 범위**입니다.
이게 NHN에 물어보려던 "점검 대상 테이블 목록"을 직접 확정한 것이 됩니다.

### 1-3. 실제 URL 형태 샘플 확인

대상으로 확인된 테이블마다 하나씩:

```sql
SELECT goodsNo,
       SUBSTRING(goodsDescription,
         LOCATE('cdn-pro-web', goodsDescription) - 8, 160) AS sample
FROM es_goods
WHERE goodsDescription LIKE '%cdn-pro-web%'
LIMIT 5;
```

**확인 목적**: 경로가 전부 `/data/img/` 로 시작하는지. 다른 패턴이 섞여 있으면 치환 규칙을 나눠야 합니다.

### 1-4. 누락 케이스 함께 확인 (NHN 쿼리가 놓치는 것)

```sql
-- 프로토콜 상대 URL
SELECT COUNT(*) FROM es_goods WHERE goodsDescription LIKE '%//cdn-pro-web%';

-- 다른 CDN 도메인
SELECT COUNT(*) FROM es_goods WHERE goodsDescription LIKE '%speedycdn%';

-- http (비암호화)
SELECT COUNT(*) FROM es_goods WHERE goodsDescription LIKE '%http://cdn%';
```

---

## 2. DB 직접 접속이 안 될 때 — 고도몰 스크립트로 조회

FTP만 있는 경우입니다.

### 2-1. 먼저 기존 파일에서 DB 호출 패턴 확보 ⭐

**이 단계를 건너뛰지 마세요.** 고도몰5의 DB API를 추측해서 쓰면 안 돌아갑니다.

이 사이트에 **이미 동작 중인 커스텀 PHP**가 있습니다. FTP로 내려받아 DB 조회 부분 3~4줄만 확인하시면 됩니다.

| 파일 | 확인된 동작 |
|---|---|
| `/goods/addtionalInfo.php` | goodsNo 배열 받아 `goodsSearchWord`, `volume` 반환 — **DB 조회 확실** |
| `/goods/tagList.php` | 태그 목록 반환 |
| `/goods/tags.php` | 태그로 상품 검색 |

`addtionalInfo.php` 를 열어서 아래 같은 부분을 찾으시면 됩니다.

```php
// 이런 형태 중 하나일 겁니다
$db = \App::load('DB');
$rows = $db->fetch($sql, true);

// 또는
$rows = \DB::fetch($sql);
```

**그 호출 방식을 그대로 2-2 스크립트에 넣으시면 됩니다.**

### 2-2. 조사 스크립트

`ProVerifyController.php` 의 라우팅 규칙을 그대로 따릅니다.

- **배치 경로**: `data/module/Controller/Front/Professional/CdnScanController.php`
- **호출 URL**: `https://www.dalba.co.kr/professional/cdn_scan.php?token=바꾸세요`
- 파일명 = 클래스명 반드시 일치 (`CdnScanController`)

```php
<?php
/**
 * CDN 절대 URL 전수 조사 (읽기 전용, 조사 후 반드시 삭제)
 * 배치: data/module/Controller/Front/Professional/CdnScanController.php
 * URL : https://www.dalba.co.kr/professional/cdn_scan.php?token=...
 */
namespace Controller\Front\Professional;

use Request;

class CdnScanController extends \Controller\Front\Controller
{
    // ⚠️ 반드시 임의 문자열로 변경. 공개 URL이므로 토큰 없으면 아무나 접근 가능
    private $token = 'CHANGE_ME_RANDOM_STRING';

    public function index()
    {
        header('Content-Type: text/plain; charset=utf-8');

        if (Request::get()->get('token') !== $this->token) {
            echo 'forbidden';
            exit;
        }

        // ── ① 여기를 addtionalInfo.php 에서 본 방식으로 교체 ──
        $db = \App::load('DB');

        // 1) 검사 대상 컬럼 수집
        $cols = $db->fetch("
            SELECT TABLE_NAME, COLUMN_NAME
            FROM information_schema.COLUMNS
            WHERE TABLE_SCHEMA = DATABASE()
              AND DATA_TYPE IN ('varchar','text','mediumtext','longtext','tinytext')
        ", true);

        // 2) 컬럼별 건수 집계
        $total = 0;
        foreach ($cols as $c) {
            $t = $c['TABLE_NAME'];
            $col = $c['COLUMN_NAME'];

            $r = $db->fetch("
                SELECT COUNT(*) AS cnt
                FROM `{$t}`
                WHERE `{$col}` LIKE '%cdn-pro-web%'
            ");

            $cnt = isset($r['cnt']) ? (int)$r['cnt'] : 0;
            if ($cnt > 0) {
                echo str_pad($t, 40) . str_pad($col, 30) . $cnt . "\n";
                $total += $cnt;
            }
        }

        echo "\n합계 행 수: {$total}\n";
        exit;
    }
}
```

> `$db->fetch($sql, true)` 는 전체 행, `$db->fetch($sql)` 는 단일 행을 가정한 것입니다.
> **2-1에서 확인한 실제 시그니처에 맞춰 조정**하세요.

### 2-3. 배포 및 실행

1. 고도몰 관리자 우측 상단 **"개발 소스 관리"** 접속
2. 위 파일 업로드 (**"운영소스로 적용하기" 누르지 말 것** — 조사용이라 스테이징이면 충분)
3. 브라우저에서 URL 호출
4. 결과 복사
5. **파일 즉시 삭제** ⚠️

---

## 3. 조사 후 판단 기준

| 확인 항목 | 의미 |
|---|---|
| 대상 테이블 목록 | NHN에 "이 범위가 맞나" 확인용. 가이드보다 넓을 것으로 예상 |
| 총 건수 | 1,000건 넘으면 수동 수정 불가 → NHN 일괄 치환 요청 필수 근거 |
| 경로 패턴 | 전부 `/data/img/` 면 치환 규칙 1개로 해결 |
| `es_goods` 외 테이블 존재 | NHN 제시 쿼리 부족하다는 실증 근거 |

---

## 4. 치환은 아직 하지 마세요

`UPDATE` 는 **NHN 답변 확인 후**입니다.

> **업그레이드 후 CDN 도메인이 실제로 변경되는가?**

**변경되지 않으면 치환 작업 자체가 불필요합니다.** 1,000건 이상 운영 DB를 건드리는 작업이라 이 답을 받기 전에 손대면 헛수고이자 불필요한 리스크입니다.

참고로 치환 시 쿼리는 이 형태가 됩니다. (실행 전 반드시 백업)

```sql
UPDATE es_goods
SET goodsDescription = REPLACE(
    goodsDescription,
    'https://cdn-pro-web-223-233.cdn-nhncommerce.com/dalbapiedmot_godomall_com',
    ''
);
```

결과: `https://cdn-pro-web-223-233.../dalbapiedmot_godomall_com/data/img/a.jpg` → `/data/img/a.jpg`

---

## 5. 보안 메모

- 조사 스크립트는 **공개 URL**에 노출됩니다. 토큰 필수, 조사 후 즉시 삭제
- 이번에 공유해주신 FTP 비밀번호는 대화 기록에 평문으로 남았습니다. 작업 종료 후 **교체 검토** 권장
- 다운로드한 고도몰 소스에 DB 접속 정보가 들어있을 수 있으니 **git 커밋 금지** (`ProVerifyController.php` 주석에도 동일한 경고가 있음)
