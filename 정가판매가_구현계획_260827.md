# 카테고리 목록 정가/판매가 조건부 노출 — 구현 계획

작성일: 2026-08-27
상태: **클라이언트 승인 완료, 착수 대기** (진열 템플릿 원문 확보가 유일한 블로커)
관련 문서: [정가노출_검토회신_260824.md](정가노출_검토회신_260824.md) (조사 과정·회신 메일 전문)

---

## 요구사항 (클라이언트 확정, 2026-08-27 승인)

카테고리 진열 페이지 상품 카드에 정가/판매가를 조건부로 함께 노출.

- 정가 = 0 → 판매가만 노출
- 정가 = 판매가 → 판매가만 노출
- 정가 > 판매가 → 정가(취소선) + 판매가 노출
- 노출 기간은 개발단에서 제어하지 않음 — 관리자 입력값 기준 자동 분기
- 대표 테스트 카테고리: `cateCd=001029` (8월 테스트 운영 중, 9/28부터 월 1회 정기 운영 예정)
- 상품 상세페이지 스타일 그대로 참고, 별도 디자인 시안 없음
- 카테고리 목록 + 동일 진열 템플릿을 쓰는 검색 결과 등 목록형 페이지 공통 적용

---

## 최종 조건문

목록과 상세 **양쪽에 동일하게** 적용한다.

```
fixedPrice > 0 && goodsPriceDisplayFl == 'y' && fixedPrice > goodsPrice
```

| 조건 | 출처 | 역할 |
|---|---|---|
| `fixedPrice > 0` | 상세 기존 로직 | 정가 미입력 상품 제외 |
| `goodsPriceDisplayFl == 'y'` | 상세 기존 로직 | 가격 비노출 설정 상품(회원 전용가 등) 제외 |
| `fixedPrice > goodsPrice` | 이번 요구사항 | 정가=판매가 케이스 제외 (할인 아닐 때 취소선 안 붙임) |

**왜 상세도 같이 고치나**: 현재 상세페이지([goods_view.html:1484](goods_view.html#L1484))는 앞의 두 조건만 있고 세 번째가 없다. 즉 정가=판매가여도 상세는 무조건 취소선이 붙는다. 목록에만 세 번째 조건을 넣으면 같은 상품인데 목록/상세 표기가 달라진다(목록: `46,000` / 상세: `~~46,000~~ 46,000`). 지금은 이 케이스가 0건이라 안 보이지만 신규 상품 등록 시 흔히 발생할 수 있어, 상세 조건문에도 동일 조건을 추가해 완전히 맞춘다.

---

## 수정 대상

### 1. 상품 진열 템플릿 (신규 추가) — ⚠️ 원문 미확보, 착수 블로커

`.idea/goods_list.html:187` 의 `{ # goodsTemplate }` 이 실체를 가리키는 자리 표시자. 실제 파일은 **고도몰 관리자 DB 저장형**이라 로컬에 없음.

현재 렌더링되는 마크업 ([goods_view.html:1484-1487](goods_view.html#L1484-L1487) 패턴을 그대로 재사용):

```html
<div class="item_money_box small_txt">
    <strong class="item_price">
        <span class="sans">KRW40,000</span>
    </strong>
</div>
```

`item_price` **앞**에 정가 블록 삽입 (의사코드, 실제 문법은 원문 확보 후 확정):

```
<!--{ ? fixedPrice > 0 && goodsPriceDisplayFl == 'y' && fixedPrice > goodsPrice }-->
<span class="item_price_fixed">{=fixedPriceTag}{=gd_global_currency_symbol()}{=gd_global_money_format(fixedPrice)}{=gd_global_currency_string()}{=fixedPriceTag2}</span>
<!--{ / }-->
```

- 취소선은 관리자 설정값 `fixedPriceTag`/`fixedPriceTag2`(현재 `<del>`)가 처리 → **하드코딩 금지**, 상세와 자동으로 일관성 유지
- 상품 변수명(`goods['...']` / `.` / `item['...']`)은 원문 확인 후 확정

**착수 전 확인할 것**: 진열 설정에 "정가 노출" 체크박스가 이미 있는지 먼저 확인. 있어도 `정가==판매가` 필터링은 안 되므로 조건문 커스터마이징은 결국 필요할 가능성이 높음.

### 2. 상품 상세페이지 (기존 조건 보강)

[goods_view.html:1484](goods_view.html#L1484)

```diff
- <!--{ ? gd_isset(goodsView['fixedPrice']) > 0  && goodsView['goodsPriceDisplayFl'] =='y' }-->
+ <!--{ ? gd_isset(goodsView['fixedPrice']) > 0  && goodsView['goodsPriceDisplayFl'] =='y' && goodsView['fixedPrice'] > goodsView['goodsPrice'] }-->
```

그 줄만 최소 수정. CLAUDE.md에 이 파일 장애 이력이 기록돼 있으므로 백업 필수, 자동 포매팅 금지, 업로드 후 `< !--`/`-- >` grep 검증.

### 3. CSS (신규 추가)

`.item_money_box .item_price_fixed` 스타일. 상세페이지 정가 스타일 기준. 목록 카드 높이 변화가 [.idea/goods_list.html:472-482](.idea/goods_list.html#L472-L482) 의 `ingredientsHeight()` 계산에 영향 없는지 확인.

---

## 영향 범위

| 페이지 | 반영 여부 | 근거 |
|---|---|---|
| 카테고리 목록 `goods_list.php` | O | 요청 대상 |
| 검색 결과 `goods_search.php` | O | 동일 템플릿, `item_money_box` 34건 확인 |
| 상품 상세 `goods_view.php` | O (조건 보강만) | 위 2번 |
| 메인 페이지 `/` | X | 커스텀 마크업, `item_gallery_type` 0건 |

---

## QA 체크리스트

| # | 케이스 | 검증 위치 | 기대 결과 |
|---|---|---|---|
| 1 | 정가 > 판매가 | `cateCd=001029` 전체(20개 전부 해당) | 정가(취소선) + 판매가 |
| 2 | 금액 최장 케이스 | 001029 / `1000000816` (700,000→299,000) | 한 줄 정렬 안 깨짐, PC/모바일 둘 다 |
| 3 | 정가 = 0 | `cateCd=001010` / `1000000817`·`1000000809`·`1000000068` | 판매가만 (현행 유지) |
| 4 | 정가 = 판매가 | 관리자 테스트 상품 임시 설정 | 목록·상세 **양쪽 다** 판매가만 |
| 5 | 가격 비노출 상품 | `goodsPriceDisplayFl='n'` 상품 | 목록에 정가 안 샘 |
| 6 | 품절 상품 | 001010 / `1000000814` | 기존 품절 표기 유지 |
| 7 | 타임세일 상품 | 해당 상품 | 타임세일가 로직과 충돌 없음 |
| 8 | 검색 결과 | `goods_search.php` | 목록과 동일 반영 |
| 9 | 상세페이지 회귀 | `1000000816` 등 기존 할인 상품 | 정가>판매가는 기존과 동일, 정가=판매가만 변화 |
| 10 | 메인 | `/` | 변화 없음 |

---

## 일정 / 공수

- 승인: 2026-08-27
- 착수/반영: 승인 당일 중 완료 목표
- 공수: 약 1~2시간 (목록 조건문 + 상세 1줄 보강 + CSS + QA)
- 다음 정기 할인: 9/28(월), 이후 월 1회 — 사전 점검 여유 충분

---

## 현재 블로커

**상품 진열 템플릿 원문 미확보.** 관리자 DB 저장형이라 로컬·FTP 어디에도 없음. 변수명을 추측해서 작성하면 목록 전체가 깨질 수 있어(CLAUDE.md 장애 이력 다수) 원문 확보 전까지 목록 템플릿 코드는 작성하지 않는다. 원문 확보되는 대로 위 1번 항목부터 착수.
